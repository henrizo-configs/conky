conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=12',
    gap_x = 10,
    gap_y = 30,
    minimum_height = 5,
    minimum_width = 5,
    maximum_width = 600,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
Agenda
${hr 0.5,10}
${execpi 60 emacs -batch -l ~/.emacs.d/init.el -eval '(org-batch-agenda "a" org-agenda-span (quote week))' | sed -n '1,22p' | sed 's/\$/S/' | sed 's/TODO/\${color green}TODO\${color white}/;s/DONE/\${color grey}DONE\${color white}/' | sed 's/my_habits\://'
}


Todos
${hr 0.5,10}
${execpi 60 cat ~/Dropbox/my_org/todos/todos_main.org | sed -nr '/(TODO|DONE|SCHEDULED|DEADLINE)/p' | sed -n '1,22p' | sed 's/TODO/\${color green}TODO\${color white}/;s/DONE/\${color grey}DONE\${color white}/' | sed -r 's/\*. //'
}
]]